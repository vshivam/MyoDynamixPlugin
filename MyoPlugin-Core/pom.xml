<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.ambientdynamix.contextplugins</groupId>
        <artifactId>MyoPlugin</artifactId>
        <version>2.0.0</version>
    </parent>
    <artifactId>MyoPlugin-Core</artifactId>
    <packaging>jar</packaging>
    <!--<packaging>apk</packaging>-->
    <organization>
        <name>Ambientdynamix</name>
    </organization>
    <name>MyoPlugin-Core</name>
    <description>MyoPlugin-Core</description>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
            </resource>
            <resource>
                <directory>src/main/resources/libs</directory>
                <filtering>false</filtering>
                <excludes>
                    <exclude>**/*.so</exclude>
                </excludes>
            </resource>
        </resources>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>com.jayway.maven.plugins.android.generation2</groupId>
                    <artifactId>android-maven-plugin</artifactId>
                    <version>${android.plugin.version}</version>
                    <extensions>true</extensions>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>2.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <filters>
                                <filter>
                                    <artifact>org.ambientdynamix.contextplugins:MyoPlugin-Datatypes</artifact>
                                    <includes>
                                        <include>**/*.class</include>
                                        <include>**/*.aidl</include>
                                    </includes>
                                </filter>
                            </filters>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <shadedArtifactAttached>false</shadedArtifactAttached>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>1.7</version>
                <executions>
                    <execution>
                        <id>move-dex</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <copy file="${project.build.directory}/classes.dex"
                                      tofile="${project.build.outputDirectory}/classes.dex"/>
                                <delete>
                                    <fileset dir="${project.build.directory}" includes="*.dex"/>
                                </delete>
                            </target>
                        </configuration>
                    </execution>
                    <execution>
                        <id>remove-shade-original-jar</id>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <delete>
                                    <fileset dir="${project.build.directory}"
                                             includes="original-${project.artifactId}-${project.version}.jar"/>
                                </delete>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- maven-bundle-plugin -->
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <version>2.3.7</version>
                <extensions>true</extensions>
                <!-- configure plugin to generate MANIFEST.MF -->
                <executions>
                    <execution>
                        <id>bundle-manifest</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>manifest</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <!-- configure plugin to support jar packaging -->
                    <supportedProjectTypes>
                        <supportedProjectType>jar</supportedProjectType>
                    </supportedProjectTypes>
                    <instructions>

                        <Export-Package>org.ambientdynamix.contextplugins.myoplugin</Export-Package>
                        <Bundle-NativeCode>libs/armeabi-v7a/libgesture-classifier.so ;
                            osname=Linux;
                            processor=arm_le;
                        </Bundle-NativeCode>
                        <Import-Package>org.ambientdynamix.api.application,
                            org.ambientdynamix.api.contextplugin,
                            org.ambientdynamix.api.contextplugin.security,
                        </Import-Package>
                        <Bundle-ClassPath>.</Bundle-ClassPath>
                        <Bundle-Activator/>
                        <Include-Resource>native=src/main/resources/libs</Include-Resource>
                        <Bundle-SymbolicName>org.ambientdynamix.contextplugins.myoplugin</Bundle-SymbolicName>
                    </instructions>
                </configuration>
            </plugin>

            <plugin>
                <groupId>com.jayway.maven.plugins.android.generation2</groupId>
                <artifactId>android-maven-plugin</artifactId>
                <version>${android.plugin.version}</version>
                <configuration>
                    <!--<includeLibsJarsFromAar>true</includeLibsJarsFromAar>-->
                </configuration>
                <executions>
                    <execution>
                        <phase>compile</phase>
                        <goals>
                            <goal>dex</goal>
                        </goals>
                        <configuration>
                            <attachJar>false</attachJar>
                            <dexNoLocals>true</dexNoLocals>
                            <sdk>
                                <path>/${env.ANDROID_HOME}</path>
                                <platform>${api-level}</platform>
                            </sdk>
                            <undeployBeforeDeploy>true</undeployBeforeDeploy>
                        </configuration>
                    </execution>
                    <execution>
                        <id>push plugin to device</id>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>push</goal>
                        </goals>
                        <configuration>
                            <sdk>
                                <platform>${api-level}</platform>
                            </sdk>
                            <push>
                                <source>${project.build.directory}/${project.artifactId}-${project.version}.jar</source>
                                <destination>/mnt/sdcard/dynamix/</destination>
                            </push>
                            <undeployBeforeDeploy>true</undeployBeforeDeploy>
                        </configuration>
                    </execution>
                    <execution>
                        <id>push plugin xml to device</id>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>push</goal>
                        </goals>
                        <configuration>
                            <sdk>
                                <platform>${api-level}</platform>
                            </sdk>
                            <push>
                                <source>${pom.basedir}/src/main/resources/MyoPluginDescriptor.xml</source>
                                <destination>/mnt/sdcard/dynamix/</destination>
                            </push>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- maven-jar-plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>2.4</version>
                <configuration>
                    <archive>
                        <!-- configure plugin to use MANIFEST.MF generated by maven-bundle-plugin -->
                        <manifestFile>${project.build.outputDirectory}/META-INF/MANIFEST.MF</manifestFile>
                        <addMavenDescriptor>false</addMavenDescriptor>
                    </archive>
                </configuration>
            </plugin>


            <!-- other plugins ... -->
        </plugins>
    </build>
    <dependencies>
        <dependency>
            <groupId>org.ambientdynamix.contextplugins</groupId>
            <artifactId>MyoPlugin-Datatypes</artifactId>
            <version>${project.parent.version}</version>
        </dependency>
        <dependency>
            <groupId>com.thalmic</groupId>
            <artifactId>myosdk-native</artifactId>
            <version>0.10.0</version>
            <type>jar</type>
        </dependency>
        <dependency>
            <groupId>com.thalmic</groupId>
            <artifactId>myosdk-classes</artifactId>
            <version>0.10.0</version>
            <type>jar</type>
        </dependency>
    </dependencies>
</project>
